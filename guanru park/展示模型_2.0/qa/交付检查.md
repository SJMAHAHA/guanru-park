# Guanru Park 2.0 · 交付检查

检查日期：2026-09-07（北京时间）。

- Blender 5.1.2 重新打开最终工程成功；主场景 8,654 个对象，无未打包且丢失的外部图片、无非有限变换数值、无检查范围内的大型家具与墙／玻璃体积交叠。此项为针对主要家具的检查，非所有细部碰撞的穷尽证明。
- 与 1.0 对照的五个关键基座、楼板和屋顶网格，其世界坐标顶点哈希、顶点数和面数一致；62 个功能区域一致。详见 `version_comparison.json`。
- 六个场景：完整日景、完整傍晚、B1 / 1F / 2F / 3F 独立楼层。21 张 PBR 贴图与三张参考图片均已打包。
- 16 张正式 PNG 均成功解码、尺寸符合预期，并已查看全部视角，核对构图、材料、室内亮度和楼层关系。全尺寸图保存在 `renders/`；渲染使用 Cycles、Metal GPU、96 采样与降噪。
- 网页 GLB 使用实际 Three.js GLTFLoader 与 Meshopt 解码通过：24 个分组、167 次几何绘制、1,953,970 三角面、19 张嵌入纹理，19,279,932 字节。图像加载使用 CPU 桩，另经 Pillow 解码验证 19 张图片均有效。
- 楼层隔离、家具显隐、屋顶揭开、三轮展开／复位、剖切方向、七个透视相机目标、20 个房间锚点的程序检查通过。
- TypeScript、限定到修改文件的 Oxlint、Git 空白差异检查与生产构建通过。本地页面 HTTP 200；模型、预览图及画廊共 26 个资源的 HTTP 响应哈希与源文件一致。
- 未执行浏览器 GPU 视觉与逐项点击测试。原生 Blender 效果图与网页实时渲染使用不同光照方式，二者不保证逐像素一致。

公开访问的最终检查保存在 `public_access_validation.json`，记录实际访问 URL、HTTP 状态及模型资源校验。
